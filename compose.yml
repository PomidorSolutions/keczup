services:
  keczup:
    build:
      context: .
    image: keczup
    container_name: keczup
    restart: unless-stopped
    env_file:
      - .env
    deploy:
      resources:
        limits:
          memory: 350M
    logging:
      options:
        max-size: 25M
